import { ReducerFile, ChildReducer } from './model';
import { State, Reduction } from '../parse/model';

export function createReducerFile(state: State, reductions: Reduction[]): void {
  return;
}
