import { ReducerAction } from '../model';
import { State, Reduction, Field, Type } from '../../parse/model';
import { Tree } from '../tree';

export function createReducerAction(reduction: Reduction, state: State, tree: Tree): ReducerAction {

}