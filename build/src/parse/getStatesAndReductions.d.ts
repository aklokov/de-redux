import { Options } from '../Options';
import { FileInfo } from './collectFiles';
export declare function getStates(options: Options, files: FileInfo[]): string[];
export declare function getReductions(files: FileInfo[]): string[];
