import { DispatcherFile } from '../../../derive/model';
export declare const dispatcherGenerator: {
    generate: (file: DispatcherFile) => string;
    generateContent: (gen: any, file: DispatcherFile) => void;
};
