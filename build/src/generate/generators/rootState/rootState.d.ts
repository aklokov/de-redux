import { RootStateFile } from '../../../derive/model';
export declare const rootStateGenerator: {
    generate: (file: RootStateFile) => string;
    generateContent: (gen: any, file: RootStateFile) => void;
};
