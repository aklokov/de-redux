import { ActionsFile } from '../../../derive/model';
export declare const actionsGenerator: {
    generate: (file: ActionsFile) => string;
    generateContent: (gen: any, file: ActionsFile) => void;
};
