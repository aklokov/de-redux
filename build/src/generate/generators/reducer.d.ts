import { ReducerFile } from '../../derive/model';
export declare const reducerGenerator: {
    generate: (file: ReducerFile) => string;
    generateContent: (gen: any, file: ReducerFile) => void;
};
